<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Color de fondo -->
    <solid android:color="#02061D"/>  <!-- Naranja -->

    <!-- Bordes redondeados (opcional) -->
    <corners android:radius="8dp"/>

    <!-- Borde (opcional) -->
    <stroke
        android:width="2dp"
        android:color="#310635"/>  <!-- Amarillo oscuro -->

    <!-- Padding interno (opcional) -->
    <padding
        android:left="8dp"
        android:right="8dp"
        android:top="8dp"
        android:bottom="8dp"/>
</shape>
